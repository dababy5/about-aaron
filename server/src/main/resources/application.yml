spring:
  application:
    name: aaron-portfolio
  web:
    resources:
      static-locations: classpath:/static/
  mail:
    host: smtp.gmail.com
    port: 587
    username: pintotamayoaaron@gmail.com
    password: "hkcw qogc rmlv pkdl"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

# OpenAI Configuration (placeholder)
openai:
  apiKey: ${OPENAI_API_KEY:}
app: 
  gmail: "pintotamayoaaron@gmail.com"
